db:
  image: mysql
  environment:
    MYSQL_ROOT_PASSWORD: mymysqlpassword
    WORDPRESS_PASSWORD: mywordpresspassword

web:

# build: web-server
  image: dragonde/wordpress-webserver
  ports:
    - "80:80"
  links:
    - db
  volumes:
    - wordpress:/www


