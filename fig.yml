db:
  image: mysql
  environment:
    MYSQL_ROOT_PASSWORD: mypassword

web:
  build: webserver
  ports:
    - "80:80"
  links:
    - db
  volumes:
    - /root/wordpress-fig/dbinit:/dbinit
    - /root/wordpress-fig/wordpress:/www
  environment:
    WORDPRESS_PASSWORD: mypassword

